import fs from 'fs';
import { PDFDocument, rgb } from 'pdf-lib';

async function devideDoublePagePDF(inputPath: string, outputPath: string) {
  // 入力PDFの読み込み

  // 新しいPDFの作成

  // 入力PDFを分割して出力用のストリームに書き込む処理

  // 新しいPDFの保存

}

devideDoublePagePDF('a.pdf', 'b.pdf').catch(console.error);

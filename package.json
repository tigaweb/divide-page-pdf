{
  "name": "divide-page-pdf",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "lint": "npx tslint --fix index.ts",
    "format": "npx tslint --fix index.ts && npx prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/node": "^22.13.9",
    "pdf-lib": "^1.17.1",
    "tslint": "^5.20.1",
    "typescript": "^5.8.2"
  }
}
